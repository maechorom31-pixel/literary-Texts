# CLAUDE.md — 작업 지침 (이 저장소에서 작업하는 모든 세션 필독)

EBS 2027 수능특강 **문학** 자율학습 웹앱. 고3이 자리에서 참고하며 공부하는 **참고서**다(걸어다니는 요약노트 아님). 쓰기 과제 금지 — 탭·클릭(O/X 판단)만. 데이터 명세는 `SCHEMA.md`.

## 0. 매 작업의 불변 절차

1. 수정/빌드 → 2. `python3 scripts/validate.py` (이슈 0 확인) → 3. `data/changelog.json` 맨 앞에 날짜·내역 추가 → 4. 커밋·푸시(`git push -u origin <작업 브랜치>`).
- **학습 기록 보존**: 진도·오답·장바구니가 qid(`jb-N`, `gi{G}-N`)로 localStorage에 저장된다. **기존 O/X 항목의 순서 변경·중간 삽입·삭제 금지. 추가는 배열 끝에만.** 텍스트 수정은 자유.
- 사용자에게 보이는 문구를 바꾸면 반드시 changelog에 기록한다(앱 상단 '📝 업데이트 내역'에서 열람됨).

## 1. 절대 규칙 (저작권·출처)

- 작품 **원문 전문 게재 금지**. 분석에 필요한 짧은 '구절'만 따옴표 인용.
- 제3자 강사·브랜드(한병훈·최서희·익힘책 등) **표기 금지**. 출처는 "EBS 수능특강" 또는 시험명("2022학년도 6월 모의평가")만.
- **가짜 기출 금지**: 실제 〈보기〉·선지가 없으면 기출을 사칭("○○ 일부 변형")하지 않는다. 기출 텍스트가 없으면 `exam`은 "EBS 수능특강의 시선" 또는 "작가 연계 · …"로.
- 출처 라벨만 있는 '기출 이력' focusPoint 금지 — 실제 기출 gichul 블록이 있을 때만 둔다.

## 2. 사실 근거 (가장 중요 — 추측 금지)

모든 해석·소재 풀이·구성·줄거리는 **EBS 원자료 또는 검증된 기출**에 근거해야 한다. 원문 미보유 상태에서 작가·시어·갈래·상징 의미를 기억으로 단정하지 말 것.

**원자료 위치와 복원** (컨테이너가 초기화되면 사라짐):
- `/tmp/munhak_sorted.txt` — EBS 수능특강 문학 전체(해제·주제·구성·전체 줄거리·본교재 문항/해설). 없으면: Google Drive MCP로 수능특강 문학 PDF(분할 업로드본)를 `download_file_content`(base64)로 받아 pymupdf `get_text(sort=True)`로 재추출.
- `data/_source/` (gitignore) — ebs_index.json(121작품 해제 색인), munhak_bank.json(문항 파싱), gichul_tonghap_index.md(기출 매핑), workbook_seonji.json(워크북 선지 원자료).
- Google Drive `전체_기출추출_통합.txt` (fileId `1WbiBCU6eYbnjwE9JD63u0ddsZxBbKS40`) — 평가원 15작품 + 고3 학평 29문항의 실제 〈보기〉·선지·정답.

**검증 방법**: `grep -n "작가명, 「작품명」" /tmp/munhak_sorted.txt`로 위치를 잡고 `sed`로 ±40줄 발췌. `{해제}`·`{구성}`·`{정답이 정답인 이유}`가 권위 있는 풀이다. **`head`로 자르면 뒷 연·장이 잘린다 — `{구성}` 블록은 끝까지 읽을 것**(과거 실수 #3).

## 3. 작품 빌드 워크플로

분석 → **크럭스 3축 도출** → 그 급소에서 O/X 출제 → 검증.

1. EBS 해제·주제·구성(줄거리)을 원자료에서 발췌해 사실 확정.
2. `crux` 3축: 서로 변별되는 진짜 급소(시점·초점화·상징 구조·전고·발화 주체·구조·태도). 주제 재진술 금지.
3. `gichul` 우선순위: **작품 자체 실제 기출 〈보기〉·선지 > 작가 연계**(작품-작품 관련성이 실제로 있을 때만, '연계 과잉' 함정 1개 포함 권장) **> EBS 수능특강의 시선**.
4. `judgments` 6개 내외: O/X 섞되 편중 금지. X는 `trap`+`why` 필수. 크럭스 기반이면 why에 `[크럭스: 축명]` 접두.
5. 구조 표지: 시·가사=`구성` 칩, 소설·산문·극=`줄거리` 칩(EBS 원문에서 옮김).
6. 신규 확장 작품은 `examScope: false`, index `order`는 마지막+1. 한 턴에 2편씩이 안정적.

## 4. O/X 품질 기준 (수능 고3)

- **매력적 오답**: 실제 구절·요소에 hook을 걸고 미묘하게 비튼다. 작품과 무관해 즉시 기각되는 일반론 금지.
- **금지 일반론**: 계절순환·자연예찬·은둔·사회고발·색채대비.
- **함정 유형 다양화** — 현재 분포가 정반대(53)·주제 왜곡(41)에 편중. 신규 작성 시 다음을 우선 활용: 시점 혼동, 서술자 신빙성, 발화 주체 바꿔치기, 인물·행위 혼동, 기능 왜곡, 범위(부분→전체) 왜곡, 인과 왜곡, 연계 과잉, 상징 무시.
- 산문 O/X가 줄거리 확인에 머물지 않게 서술 방식·시점·상징 기능을 겨눌 것.
- 문학 용어는 정의대로: 설의(답을 알면서 묻는 의문)≠영탄, 반어(표리 상반)≠역설(모순 속 진실), 풍자≠해학, 대구(통사 짝)≠대조(의미 대립). 의성어·의태어는 '음성 상징어'로 명시 가능.
- 어투는 EBS 해설체(평서형: 드러낸다·형상화한다·환기한다). 구어("꼬집는다·엉뚱한·속속들이" 류) 금지. 단 **작품 원문 인용 구절은 그대로 보존**.

## 5. 과거 실수 카탈로그 (재발 방지)

| # | 실수 | 예방 규칙 |
|---|---|---|
| 1 | '엄동설한 칼별'=거듭난 민중으로 단정(EBS는 '희망') | 소재의 'X=Y' 글로스는 반드시 EBS 문항 해설 원문과 대조 후 기입 |
| 2 | 007 〈보기〉에 008 도입부가 복붙 오염 | bogi 작성 후 다른 작품 것과 첫 문장 중복 여부 확인 |
| 3 | `head`로 잘라 004·007·008의 뒷 연 구성 누락 | {구성} 블록은 끝까지 발췌, 연 수를 원문과 대조 |
| 4 | "2014 예비시행 (일부 변형)" 가짜 기출 합성 | §1 — 실제 텍스트 없으면 기출 사칭 금지 |
| 5 | 출처 라벨만 남긴 '기출 이력' | 실제 기출 gichul 있을 때만 이력 카드 |
| 6 | '월북한 오빠'로 EBS보다 한 발 더 단정 | 해제가 말한 수준까지만 서술(추정 금지) |
| 7 | 설의를 '영탄'으로 표기 | §4 용어 정의 준수 |
| 8 | 같은 작품에 동일 유형 함정 3개 중복(꾀꼴꾀꼴) | 한 작품 내 함정 유형 중복 최소화 |

## 6. 남은 로드맵

- **빌드 대기열(순서)**: 사과밭을 지나며(나희덕) → 해벽(이문구) → 카인의 후예(황순원) → 잘 익은 사과(김혜순) → 산에 대하여(신경림) → 하나씩의 별(이용악) → … 최종적으로 EBS 121작품 전체. 기출 매핑은 `data/_source/gichul_tonghap_index.md`와 `docs/기출-출제이력.md` 참고.
- 사용자가 기출 문제지 텍스트(〈보기〉+선지+정답)를 주면 해당 작품 gichul을 실제 기출로 교체/추가.
- 개선 방향: 함정 유형 다양화(§4), 산문 O/X 심화, 신규 작품 `source.page` 확정(현재 0), `docs/` 대조표 갱신(현재 33작품 기준으로 낡음).

## 7. 사용자 협업 관례

- 사용자의 "그래/이어서"는 직전에 제안한 다음 단계를 진행하라는 뜻 — 재확인하지 말 것.
- 배치 작업은 2~5편 단위로 끊고, 매 배치마다 표로 간결히 보고.
- 앱은 GitHub Pages로 작업 브랜치에서 배포 중 — 푸시하면 곧 라이브에 반영된다.
- 검토(품질 검증)도 요청 없이 맡아서 하되, 원문 미보유로 인한 의심은 반드시 원자료 대조로 확정한 뒤에만 수정한다.
